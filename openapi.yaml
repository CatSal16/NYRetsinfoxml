openapi: 3.1.0
info:
  title: Retsinfo-scraper (med Google Accessionsnummer)
  version: 1.1.0
  description: GPT-handlinger til at søge og læse XML-baseret lovgivning fra retsinformation.dk ved hjælp af accessionsnummer fra Google.
servers:
  - url: https://nyretsinfoxml.onrender.com
paths:
  /search_accession:
    post:
      operationId: searchAccession
      summary: Søg accessionsnummer via Google
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                query:
                  type: string
                  description: Søgeord, fx 'køreprøve', 'syn', 'fartgrænser'
              required:
                - query
      responses:
        '200':
          description: Returnerer accessionsnummer eller fejl
  /extract_text:
    post:
      operationId: extractText
      summary: Udtræk tekst fra XML-side på retsinformation.dk
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                url:
                  type: string
                  description: Direkte URL til XML-siden for dokumentet
              required:
                - url
      responses:
        '200':
          description: Den udtrukne tekst fra XML-dokumentet
